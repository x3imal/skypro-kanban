# SkyPro Kanban — README

Короткий гайд по запуску, архитектуре и работе с кодовой базой.

## ⚡️ Быстрый старт

```bash
# 1) Установить зависимости
npm i

# 2) Запустить дев-сервер (Vite/CRA — стандартные скрипты)
npm run dev

# 3) Сборка продакшн
npm run build

# 4) Превью собранной версии (если есть)
npm run preview
```

> Если скрипты отличаются — смотрите `package.json` вашего проекта.

## ✅ Требования

- Node.js 18+ / 20+
- npm 9+
- Бэкенд API: `https://wedev-api.sky.pro/api` (см. `src/services/http.js`)

## 🌐 Маршруты

- `/` — главная (канбан-доска)
- `/task/new` — создание задачи (попап)
- `/task/:id` — просмотр/редактирование задачи (попап)
- `/login` — вход
- `/register` — регистрация
- `/exit` — подтверждение выхода (попап)
- `*` — 404

## 🧱 Архитектура (ключевые файлы)

```
src/
  components/
    Calendar/Calendar.jsx
    Card/Card.jsx
    Column/Column.jsx
    ColumnList/ColumnList.jsx
    Header/Header.jsx
    Main/Main.jsx
    PopBrowse/PopBrowse.jsx
    PopExit/PopExit.jsx
    PopNewCard/PopNewCard.jsx
    PopUser/PopUser.jsx
  context/
    AuthContext.jsx
    TasksContext.jsx
    ThemeModeContext.jsx
  pages/
    MainPage.jsx
    Login.jsx
    Register.jsx
    NotFound.jsx
  services/
    http.js
    auth.js
    kanban.js
  constants/
    calendar.js
    statuses.js
  utils/
    date.js
    status.js
    normalizeKey.js
  styles/
    global.js
    theme.js
  routes/
    ProtectedRoute.jsx
  App.jsx, AppRoutes.jsx, main.jsx
```

### Контексты

- **AuthContext** — авторизация, токен, `login/register/logout`, `withAuth`, `listUsers`.
- **TasksContext** — обёртка над `useTasks(token)` с CRUD-операциями.
- **ThemeModeContext** — переключение темы (`light`/`dark`), сохраняется в `localStorage`, интеграция со `styled-components/ThemeProvider`.

### Компоненты (по роли)

- **Header** — логотип, создание задачи, меню пользователя (переключатель темы, выход).
- **Main** — оболочка доски: показывает лоадер / пустое состояние / список колонок.
- **ColumnList** — рендерит `Column` для каждого статуса и фильтрует карточки.
- **Card** — карточка задачи.
- **Calendar** — мини‑календарь выбора даты (есть `readOnly`).
- **PopBrowse** — просмотр/редактирование задачи (статус, описание, дата).
- **PopNewCard** — создание задачи (валидирует обязательные поля).
- **PopExit** — подтверждение выхода.
- **PopUser** — попап пользователя (имя, переключатель темы, выход).

### Страницы

- **MainPage** — главная логика доски и попапов, навигация и CRUD.
- **Login / Register** — формы авторизации/регистрации.
- **NotFound** — 404, использует активную тему.

## 🗂 Данные и модели

### Задача (`task`)

```ts
type Task = {
  id: string;
  title: string;
  topic: "Web Design" | "Research" | "Copywriting";
  status: "Без статуса" | "Нужно сделать" | "В работе" | "Тестирование" | "Готово";
  date: string;      // формат 'DD.MM.YY' (для UI)
  rawDate: string|null; // ISO 'YYYY-MM-DD' (оригинал для API)
  description: string;
  userId: string|null;
}
```

Маппинг: `mappers/task.js -> mapTask()` нормализует данные (статус/даты/фолбэки).

### Статусы

- Список по умолчанию: `constants/statuses.js`
- Конвертация: `utils/status.js` — `normalizeStatus` (в человеко‑читаемый), `statusToApi` (в ключ API).

### Даты

- `utils/date.js`: `buildMonthGrid()`, `formatDate()`, `toISODateOnly()`
- `constants/calendar.js`: названия дней/месяцев, парсинг строковых дат и ISO.

## 🔌 API-слой

Базовый URL: `services/http.js` → `BASE_URL = https://wedev-api.sky.pro/api`.

### Авторизация (`services/auth.js`)

- `authApi.register({ login, name, password })` → `POST /user`
- `authApi.login({ login, password })` → `POST /user/login`
- `authApi.listUsers(token)` → `GET /user`

### Канбан (`services/kanban.js`)

- `kanbanApi.list(token)` → `GET /kanban`
- `kanbanApi.getById(id, token)` → `GET /kanban/:id`
- `kanbanApi.create(task, token)` → `POST /kanban`
- `kanbanApi.update(id, patch, token)` → `PUT /kanban/:id`
- `kanbanApi.remove(id, token)` → `DELETE /kanban/:id`

> Токен добавляется в заголовок `Authorization: Bearer ...` в `http.js`.

## 🎨 Темизация

- Темы определены в `styles/theme.js`: `light`, `dark`.
- Переключение — через `ThemeModeContext` (`useThemeMode()`): `{ mode, isDark, toggle }`.
- Глобальные стили — `styles/global.js` (фон/цвет текста реагируют на тему).

## 🧪 JSDoc

В ключевых файлах добавлены JSDoc‑комментарии (компоненты, страницы, контексты, хуки, сервисы, утилиты).  
Цель — краткие описания пропсов и возвратов без избыточной детализации.

## ♻️ Типичный поток данных (пример)

1. `Login/Register` → `AuthContext.login/register` → сохраняется `token` в `localStorage`.
2. `TasksContext` вызывает `useTasks(token)` → `kanbanApi.list()` → `cards` попадают в `MainPage`.
3. `Main` → `ColumnList` → `Card` (фильтрация по статусам).
4. `PopNewCard` → `useTasks.create()` (оптимистичный апдейт, затем подтверждение с API).
5. `PopBrowse` → `useTasks.update/remove()`.

## 🛠 Настройки и кастомизация

- **Смена API**: обновите `BASE_URL` в `services/http.js`.
- **Статусы**: меняйте `constants/statuses.js` и соответствующий маппинг в `utils/status.js`.
- **Категории**: для `PopNewCard`/`useTasks` список тем ограничен `["Web Design","Research","Copywriting"]` — правьте в `useTasks.js` (`ALLOWED_TOPICS`).

## 👤 Пользовательский сценарий: как пользоваться Kanban-доской

### 1. Вход в систему
1. Откройте приложение и перейдите на страницу **входа** (`/login`).
2. Введите логин (эл. почту) и пароль, затем нажмите **«Войти»**.
3. Если у вас нет аккаунта — перейдите по ссылке **«Регистрируйтесь здесь»**, заполните форму и нажмите **«Зарегистрироваться»**.  
   После регистрации вы автоматически войдёте в систему.

### 2. Главная страница (доска задач)
После входа откроется **главная страница** с колонками задач:
- **Без статуса**
- **Нужно сделать**
- **В работе**
- **Тестирование**
- **Готово**

💡 Если задач ещё нет, появится сообщение **«Пока нет карточек»**.

### 3. Создание задачи
1. В верхней панели нажмите кнопку **«Создать новую задачу»**.
2. Откроется окно **«Создание задачи»**:
    - Введите **название** и **описание**.
    - Выберите **дату выполнения** в календаре (опционально).
    - Укажите **категорию**: *Web Design*, *Research* или *Copywriting*.
3. Нажмите **«Создать задачу»** — задача появится в колонке **«Без статуса»**.

### 4. Просмотр и редактирование задачи
1. Нажмите на кнопку с **тремя точками** в карточке задачи.
2. Откроется попап **просмотра задачи**:
    - Отображается статус, описание и дата.
3. Чтобы изменить задачу:
    - Нажмите **«Редактировать задачу»**.
    - Измените статус, описание или дату.
    - Сохраните изменения кнопкой **«Сохранить»**.

### 5. Удаление задачи
1. В окне задачи нажмите **«Удалить задачу»**.
2. После подтверждения задача исчезнет с доски.

### 6. Переключение темы
1. В правом верхнем углу нажмите на имя пользователя.
2. В открывшемся меню включите или выключите **тёмную тему** с помощью чекбокса **«Тёмная тема»**.
3. Тема сохраняется автоматически и не сбрасывается при перезагрузке.

### 7. Выход из аккаунта
1. В меню пользователя нажмите **«Выйти»**.  
   Или перейдите по маршруту `/exit`.
2. Подтвердите выход кнопкой **«Да, выйти»**.
3. Вас перенаправит на страницу **входа**.

### 8. Ошибки и особые состояния
- При потере соединения появится сообщение об ошибке загрузки задач.
- Если запрос не прошёл — повторите действие после обновления страницы.
- При первой загрузке доски отображается сообщение **«Данные загружаются…»**.

#### 🧭 Краткий сценарий (в 3 шага)

| Этап | Действие | Результат |
|------|-----------|------------|
| 1 | Войти или зарегистрироваться | Появляется пустая доска |
| 2 | Создать задачу | Новая карточка добавлена в колонку |
| 3 | Изменять статусы, редактировать и удалять | Доска обновляется в реальном времени |
